import {Navigate,Route,Routes} from 'react-router-dom';import {useAuth} from './context/AuthContext';import Login from './pages/Login';import SuperAdminLayout from './layouts/SuperAdminLayout';import RestaurantLayout from './layouts/RestaurantLayout';import Restaurants from './pages/super-admin/Restaurants';import Dashboard from './pages/restaurant/Dashboard';
function Guard({role,children}){const {user,loading}=useAuth();if(loading)return <div className="p-8">Loading…</div>;if(!user)return <Navigate to="/login" replace/>;if(role&&user.role!==role)return <Navigate to={user.role==='SUPER_ADMIN'?'/super-admin/restaurants':'/app'} replace/>;return children}
const Placeholder=({name})=><div><h2 className="text-2xl font-bold">{name}</h2><p className="text-slate-500 mt-2">Module placeholder for the next implementation slice.</p></div>;
export default function App(){return <Routes><Route path="/login" element={<Login/>}/><Route path="/super-admin" element={<Guard role="SUPER_ADMIN"><SuperAdminLayout/></Guard>}><Route path="restaurants" element={<Restaurants/>}/></Route><Route path="/app" element={<Guard role="RESTAURANT"><RestaurantLayout/></Guard>}><Route index element={<Dashboard/>}/>{['orders','kot','billing','menu','purchases','expenses','salaries','reports','settings'].map(x=><Route key={x} path={x} element={<Placeholder name={x[0].toUpperCase()+x.slice(1)}/>}/>)}</Route><Route path="*" element={<Navigate to="/login" replace/>}/></Routes>}
