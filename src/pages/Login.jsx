import {useState} from 'react';import {Navigate,useNavigate} from 'react-router-dom';import {useAuth} from '../context/AuthContext';
export default function Login(){const {user,login}=useAuth();const nav=useNavigate();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);if(user)return <Navigate to={user.role==='SUPER_ADMIN'?'/super-admin/restaurants':'/app'} replace/>;async function submit(e){e.preventDefault();setError('');setBusy(true);try{const u=await login(email,password);nav(u.role==='SUPER_ADMIN'?'/super-admin/restaurants':'/app')}catch(e){setError(e.response?.data?.message||'Login failed')}finally{setBusy(false)}}return <div className="min-h-screen grid place-items-center p-4"><form onSubmit={submit} className="w-full max-w-sm bg-white border rounded-xl p-6 shadow-sm"><h1 className="text-xl font-bold">Restaurant Billing</h1><p className="text-sm text-slate-500 mt-1 mb-6">Sign in to continue</p>{error&&<div className="bg-red-50 text-red-700 p-3 rounded mb-4 text-sm">{error}</div>}<label className="block text-sm mb-1">Email</label><input value={email} onChange={e=>setEmail(e.target.value)} type="email" className="w-full border rounded px-3 py-2 mb-4" required/><label className="block text-sm mb-1">Password</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password" className="w-full border rounded px-3 py-2 mb-6" required/><button disabled={busy} className="w-full bg-slate-900 text-white rounded px-3 py-2 disabled:opacity-50">{busy?'Signing in…':'Sign in'}</button></form></div>}
